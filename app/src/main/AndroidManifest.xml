<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="au.mgemployeehire.employeehirehrpanel">

    <application
        android:allowBackup="true"
        android:icon="@drawable/hrlogo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/hrlogo"
        android:supportsRtl="true"
        android:theme="@style/Theme.EmployeeHireHRPanel">
        <activity android:name=".JobPositionSelectionActivity">
<!--            <intent-filter>-->
<!--                <action android:name="android.intent.action.MAIN" />-->

<!--                <category android:name="android.intent.category.LAUNCHER" />-->
<!--            </intent-filter>-->
        </activity>
        <activity android:name=".ApplicantsOfJobActivity" />
        <activity android:name=".JobAdvertisementActivity" />
        <activity android:name=".SigninActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignupActivity" />
        <activity android:name=".AllJobPostActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>